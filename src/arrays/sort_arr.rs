pub fn sort_array() {
    let mut arr = [5, 2, 8, 1, 4];
    print!("<-------------Program to Sort an Array----------->\n\n");
    println!("original array: {:?}\n\n", arr);
    // Sort the array in ascending order
    arr.sort();
    println!("Sorted array: {:?}\n\n", arr);
}
